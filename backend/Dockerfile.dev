FROM python:3.14-slim

ARG UID=1000
ARG GID=1000

# Create a group and user to run the application
RUN groupadd -o -g ${GID} app && \
    useradd -m -o -u ${UID} -g app app

WORKDIR /app

# Install system dependencies
RUN apt-get update && apt-get install -y --no-install-recommends \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Install uv package manager
RUN pip install --no-cache-dir uv

# Fix ownership
RUN chown -R app:app /app

# Switch user BEFORE installing dependencies
USER app

# Copy development entrypoint
COPY --chown=app:app scripts/entrypoint.dev.sh /app/scripts/entrypoint.dev.sh
RUN chmod +x /app/scripts/entrypoint.dev.sh

ENTRYPOINT ["/app/scripts/entrypoint.dev.sh"]
